name: 测试LinuxArm架构和X64架构

on:
  push:


jobs:
  build:
  runs-on: ubuntu-latest
  strategy:
      matrix:
        arch:
          - arm64
          - x86
  env:
      BUILD_TARGET: linux
      BUILD_ARCH: ${{ matrix.arch }}
  steps:
    - name: 测试环境
    shell: pwsh
    run: |
        uname -a